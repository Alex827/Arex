#just calls the check new files function
#written this way because easier to be called in bash
from everything import parseBoth
from everything import checkForNewFiles

parseBoth()
checkForNewFiles()

#just calls the find problem function
#written this way because easier to be called in bash
from everything import parseBoth
from everything import findProblematicFiles

parseBoth()
findProblematicFiles()
